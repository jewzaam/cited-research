# Command Encapsulation Bypass Citations

Generated By: Claude Code (Claude Opus 4.6)

All sources discovered via WebSearch on 2026-03-27.

---

[EB-1] Anthropic. "Claude Code Hooks Documentation."
URL: https://code.claude.com/docs/en/hooks
Data extracted: PreToolUse hooks receive JSON payload with `tool_input.command` as a string. Hook handlers pattern-match against this string. No mechanism exists to inspect child processes or subshell commands. Example blocker script uses `jq -r '.tool_input.command'` then `grep -q 'rm -rf'`.
Quality: High (official Anthropic documentation)

[EB-2] Anthropic. "Claude Code Sandboxing Documentation."
URL: https://code.claude.com/docs/en/sandboxing
Data extracted: OS-level sandbox (Seatbelt macOS, bubblewrap Linux) explicitly applies to child processes. Direct quote: "These OS-level restrictions ensure that all child processes spawned by Claude Code's commands inherit the same security boundaries." Under network isolation specifically: "Comprehensive coverage: Restrictions apply to all scripts, programs, and subprocesses spawned by commands." Escape hatch: `dangerouslyDisableSandbox`. Network sandboxing "does not otherwise inspect the traffic passing through the proxy." Sandbox enabled via `/sandbox` command.
Quality: High (official Anthropic documentation)
⚠️ CORRECTED: The "Restrictions apply to all scripts..." quote is specifically about network isolation, not a general sandbox statement. Filesystem isolation also applies to subprocesses (stated separately) but the quote's scope was misrepresented.

[EB-3] NVIDIA AI Red Team. "Practical Security Guidance for Sandboxing Agentic Workflows." January 30, 2026.
URL: https://developer.nvidia.com/blog/practical-security-guidance-for-sandboxing-agentic-workflows-and-managing-execution-risk/
Data extracted: "Attackers often use indirection -- calling a more restricted tool through a safer, approved one -- as a common way to bypass application-level controls such as allowlists." "Once control passes to a subprocess, the application has no visibility into or control over the subprocess." Also warns: "many agentic systems only apply sandboxing at the time of tool invocation. While this prevents a wide range of abuse, many agentic functionalities default to running outside the sandbox, including hooks, MCP configurations that spawn local processes, and scripts used by 'skills.'"
Quality: High (NVIDIA AI Red Team, established vendor)
⚠️ Full text not directly fetched; quotes from search summaries corroborated by multiple outlets

[EB-4] David Wong (zkSecurity). "Weaponizing AI Assistants: With Their Permission."
URL: https://www.cryptologie.net/posts/weaponizing-ai-assistants-with-their-permission/
Data extracted: Discusses READMEs and project config files (CLAUDE.md, GEMINI.md, AGENTS.md) as attack vectors against AI coding agents. Identifies that each CLI has a YOLO-mode permission-bypass flag (Claude: `--dangerously-skip-permissions`, Codex: `--dangerously-bypass-approvals-and-sandbox`, Gemini: `-y`). Conceptual warning about poisoned repos triggering agents. Quote: "Now I'm wondering if every README is just one cleverly worded preprompt away from becoming a remote shell script."
Quality: Medium-High (author is former Facebook/Libra security lead; full text not directly fetched)
⚠️ CORRECTED: Original citation described a "PoC [that] detects available AI CLIs and invokes them" — this overstated the source. Wong identifies the flags and discusses the risk conceptually but does not describe a specific automated PoC tool. Makefiles are not specifically mentioned as attack vectors in Wong's post — that concept comes from the encapsulation bypass analysis itself.

[EB-5] Snyk. "Weaponizing AI Coding Agents for Malware in the Nx Malicious Package."
URL: https://snyk.io/blog/weaponizing-ai-coding-agents-for-malware-in-the-nx-malicious-package/
Data extracted: Real-world incident where Nx malicious package weaponized local AI coding agents (Claude, Gemini, Q) via prompts to inventory sensitive files and exfiltrate data.
Quality: High (established security vendor, real-world incident)

[EB-6] GitHub. "Copilot CLI - Allowing and Denying Tool Use" + Feature Request #2037.
URLs: https://docs.github.com/en/copilot/how-tos/copilot-cli/allowing-tools, https://github.com/github/copilot-cli/issues/2037
Data extracted: Uses `shell(command:*)` glob patterns. Supports sub-command matching for some commands (e.g., `shell(git push)` blocks `git push` specifically). Feature request #2037 (user-filed, not official docs) identifies a gap: allowing `shell(deno:*)` (primary example) or npm also permits install subcommands with no way to restrict to specific ones.
Quality: Medium-High (official docs + community feature request — distinct sources)
⚠️ CORRECTED: Original citation conflated official docs with a user feature request under a single URL. Copilot CLI does have partial sub-command matching for git/gh, not "top-level command string only." Issue #2037 uses deno as primary example, not npm.

[EB-7] OpenAI. "Codex Agent Approvals & Security."
URL: https://developers.openai.com/codex/agent-approvals-security
Data extracted: Two-layer model: approval system inspects top-level command, sandbox uses OS-native primitives (Seatbelt, Bubblewrap/Landlock, Restricted Tokens). "Spawned subagents now inherit sandbox and network rules more reliably."
Quality: High (official OpenAI documentation)

[EB-8] agentsh. "Secure Sandbox."
URL: https://www.agentsh.org/docs/secure-sandbox/
Data extracted: Kernel-level enforcement using seccomp, FUSE, ptrace, and network proxy as alternative mechanisms for different environments. Replaces /bin/bash with policy-enforcing shim.
Quality: Medium (third-party tool, not directly fetched)
⚠️ CORRECTED: Original citation claimed "intercepts all child process commands" — source describes intercepting system calls, not specifically child process commands. Original claimed compatibility with "Claude Code, Codex CLI, Cursor" — this comes from a sibling product (agentsys) not the secure sandbox docs. Original described mechanisms as a single pipeline; source presents them as alternatives for different environments.

[EB-9] Trail of Bits. "claude-code-config" PostToolUse hooks.
URL: https://github.com/trailofbits/claude-code-config
Data extracted: "PreToolUse hooks are for prevention (block dangerous commands). PostToolUse hooks are for observation (log what happened)." PostToolUse cannot prevent actions. Explicit warning: "Hooks are not a security boundary -- a prompt injection can work around them."
Quality: High (established security firm, public repo)
