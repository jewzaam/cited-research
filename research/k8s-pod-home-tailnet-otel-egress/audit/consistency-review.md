<!-- Generated By: Claude Code (Claude Opus 4.7) -->

# Consistency Review

Cross-file consistency audit: version numbers, ports, CIDRs, logical
consistency between recommendations, link validity, citation numbers.

## Findings

| Issue | Files | Severity | Details | Action |
|---|---|---|---|---|
| Missing `audit/` files when README links to them | README.md | FAIL | README's "Full analysis" section links to `[audit/](audit/)` but the directory was created empty during initial scaffolding. | **Status: RESOLVED** — `audit/citation-audit.md` and `audit/consistency-review.md` written. |
| Citation [29] never referenced inline | citations.md | WARN | `[29]` defined but no inline `[29]` in any deliverable file. | **Status: RESOLVED** — added inline `[29]` next to the kube-router NPC claim in `networkpolicy-otel-patterns.md` and `synthesis.md`. |
| `kubernetes.io/metadata.name` 1.22 version not in citations | networkpolicy-otel-patterns.md | WARN | "Stable since k8s 1.22" is from agent research, not primary fetch. | **Status: ACCEPTED** — claim is hedged inline as "(per agent research; agent's source snippet from kubernetes.io confirms control-plane-managed nature)" and the immutability claim is supported by [23]. Version-specific fact remains a known gap. |
| `DelegateFSGroupToCSIDriver` versions in citation but not body | pvc-permissions-non-root.md | WARN | citations.md [8] lists alpha 1.22, beta 1.25, GA 1.26; body says only "(since 1.26)". | **Status: ACCEPTED** — body text correctness preserved; alpha/beta milestones are surfaced in the citation entry for readers who want them. Not surfacing in body avoids cluttering the recommendation. |
| `fsGroupChangePolicy` 1.20 wording mismatch | analysis.md vs pvc-permissions-non-root.md | WARN | analysis.md says "1.20"; pvc-permissions-non-root.md says "1.20 (beta)". | **Status: ACCEPTED** — both are accurate; the introduction was a beta in 1.20 per [7]. The reference file is more precise; the analysis summary is less precise but not wrong. |
| Synthesis softens "one StatefulSet per operator" vs README's firmer table | README.md vs synthesis.md | WARN | README firmly recommends; synthesis says "At N=1, a Deployment+PVC is functionally equivalent and slightly simpler." | **Status: ACCEPTED** — README is the decision-tool, synthesis is the rationale. The synthesis caveat is correct (N=1 makes the controllers near-equivalent) and the README captures the recommendation; the soft drift is intentional. |

## Verified consistent

- `persistentVolumeClaimRetentionPolicy` alpha 1.23 / beta 1.27 — uniform across all files
- 40-hour fsGroup chown attribution to k/k #118058 — uniform
- 2-3 Mbit/s vs 700-800 Mbit/s figures (tailscale #16820) — uniform
- Tailscale MTU 1280, workaround MTU 1200 — uniform
- OTLP ports: 4317 (gRPC), 4318 (HTTP) — uniform
- k3s default Pod CIDR 10.42.0.0/16, Service CIDR 10.43.0.0/16 — uniform
- Longhorn 1.11 version references — uniform
- All inline `[N]` citation numbers in [1..28, 30] now resolve to citations.md entries (and [29] too after correction)
- Cross-file recommendations do not contradict:
  - D2 restricted PSS is compatible with D1 PVC pattern (PVC is in the
    restricted allow-list per [5])
  - D3 in-cluster recommendation matches D4 Pattern B target (the
    in-cluster Collector Service in the `otel` namespace)
  - Synthesis bullets correctly aggregate per-dimension verdicts
- Markdown relative links (`citations.md`, `references/*.md`, `audit/`)
  all resolve to existing files post-correction
- Quote precision verified across files for:
  - "Deleting and/or scaling a StatefulSet down..."
  - "currently in beta"
  - "continuously resend data until it times out"
  - "Use caution when deleting a PVC"
  - "A limitation of Azure CNI Powered by Cilium..."

## Summary

- FAIL: 0 (after corrections)
- WARN: 0 open (4 WARN accepted with rationale, 1 WARN resolved)
